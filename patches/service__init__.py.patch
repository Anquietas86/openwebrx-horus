# Add to owrx/service/__init__.py
#
# In the _getSecondaryDemodulator() method, add Horus to the mode mapping.
# Place this alongside the existing sonde/packet entries.
#
# Uses inline imports (matching the existing pattern in this file) to avoid
# needing owrx.chain to be importable at module load time.

# Add these conditions in _getSecondaryDemodulator(), before the raise ValueError:

elif modulation == "horus_binary":
    from owrx.chain.horus import HorusDemodulatorChain
    return HorusDemodulatorChain(mode_str="horus_binary")
elif modulation == "horus_rtty":
    from owrx.chain.horus import HorusDemodulatorChain
    return HorusDemodulatorChain(mode_str="horus_rtty")
